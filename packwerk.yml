# See: Setting up the configuration file
# https://github.com/Shopify/packwerk/blob/main/USAGE.md#setting-up-the-configuration-file

# List of patterns for folder paths to include
# include:
# - "**/*.{rb,rake,erb}"

# List of patterns for folder paths to exclude
# exclude:
# - "{bin,node_modules,script,tmp,vendor}/**/*"

# Patterns to find package configuration files
# package_paths: "**/"

# List of application load paths
# These load paths were auto generated by Packwerk.
load_paths:
- components/app_base/app/channels
- components/app_base/app/controllers
- components/app_base/app/controllers/concerns
- components/app_base/app/helpers
- components/app_base/app/jobs
- components/app_base/app/mailers
- components/app_base/app/models
- components/app_base/app/models/concerns
- components/app_base/app/views

# List of custom associations, if any
# custom_associations:
# - "cache_belongs_to"

# Location of inflections file
# inflections_file: "config/inflections.yml"
